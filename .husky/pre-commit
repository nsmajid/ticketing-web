#!/usr/bin/env sh

echo "🔍 Running lint-staged in Docker..."

docker compose exec -T frontend pnpm lint-staged

RESULT=$?

if [ $RESULT -ne 0 ]; then
  echo ""
  echo "❌ Commit aborted."
  exit $RESULT
fi

echo ""
echo "✅ Code quality check passed."
